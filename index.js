const head   = require('./head');
const tail   = require('./tail');
const middle = require('./middle');
const assertArrayEqual = require
module.exports = {
  head:   head,
  tail:   tail,
  middle: middle
};